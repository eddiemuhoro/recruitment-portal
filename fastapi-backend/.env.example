# Production Database (Supabase)
DATABASE_URL=postgresql://username:password@host:port/database

# Local Development Database (for docker-compose)
# DATABASE_URL=postgresql://postgres:password@db:5432/recruitment_db

# Authentication
SECRET_KEY=your-secret-key-here-generate-a-secure-one
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Local PostgreSQL Settings (for docker-compose)
POSTGRES_DB=recruitment_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# API Keys
OPENAI_API_KEY=your_openai_api_key_here

# Redis Configuration
REDIS_URL=redis://redis:6379

# Vercel will automatically set these environment variables in production
# You need to configure them in your Vercel dashboard:
# 1. Go to your Vercel project settings
# 2. Navigate to Environment Variables
# 3. Add each variable listed above with your actual values
